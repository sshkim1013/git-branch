# git branch 명령어 정리

- 브랜치 확인
  - git branch 

- 브랜치 생성
  - git branch {브랜치명}

- 브랜치 전환
  - git checkout {브랜치명}
    - `checkout -b` : 생성하면서 전환
  - git switch {브랜치명}
    - `switch -c` : 생성하면서 전환

- 브랜치 삭제
  - git branch -d {브랜치명}

